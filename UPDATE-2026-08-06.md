# 点歌插件性能与容灾升级说明（2026-08-06）

本插件的部分更新内容与原因说明。

## 背景

有使用者反馈：将插件部署到**云服务器 / 海外主机**后，群聊中 **网易云、QQ 音乐无法使用**，仅 **酷狗** 正常；而开发者本地（家用网络）访问所有源均正常，且服务器上可以 ping 通网易云。

排查结果为 **音乐平台对服务器出口 IP 的风控策略不同**：

| 平台 | 对 IDC / 云端出口 IP | 表现 |
| --- | --- | --- |
| 网易云 | 风控严格 | 搜索无结果 / 返回空列表 |
| QQ 音乐 | 风控严格 | 搜索无结果 / 无法获取直链 |
| 酷狗 | 较为宽松 | 基本可用 |
| 酷我 | 较为宽松 | 基本可用 |

`ping` 只检测 ICMP 连通性，无法代表 HTTP API 是否放行，因此"能 ping 通网易云"与"网易云用不了"并不矛盾。

---

## 本次改动

### 1. 新增代理配置（`proxy`）

针对服务器 / 云主机风控问题，新增 `proxy` 配置项。

- 配置位置：插件配置面板或 `_conf_schema.json`
- 支持格式：`http://127.0.0.1:7890`、`socks5://127.0.0.1:1080` 等
- 作用：所有音乐源请求均走代理出口，可将请求伪装为住宅/本地出口 IP，绕过网易云、QQ 音乐对 IDC 出口的风控
- 支持热更新：修改后无需重启，可通过 `/song set proxy <代理地址>`

对应实现：`sources/__init__.py` 的 `SourceManager._apply_proxy()`。

### 2. 请求头加固（UA / Referer / Cookie）

为降低各平台风控触发率，统一加强请求头：

- 会话级默认请求头：`Accept`、`Accept-Language`
- 音乐源通用 UA：桌面 Chrome 完整 UA
- 各源独立设置 `Referer`（网易云、QQ 音乐、酷狗、酷我等均已在源码中补充）
- QQ 音乐补充 `Origin` 与基础 Cookie；网易云补充 `Cookie` 与 `Referer`、`appver`
- B 站源补充播放接口 `Referer`

实现位置：各源文件的 `_headers()` 方法及 `sources/__init__.py`

### 3. 专属日志功能

新增插件**独立日志文件**，方便排查各源被风控的具体原因。

- 日志文件位置：`plugin_data/astrbot_plugin_music_custom/logs/music.log`
- 单文件占用 2MB 自动滚动，保留 3 份备份
- 日志按源划分：搜索成功/无结果/被风控（HTTP 状态码、错误码）、直链获取结果、语音/卡片发送结果
- 同时转发一份到 AstrBot 主日志/WebUI，方便在线查看

实现位置：`log.py`（`get_logger()` / `get_log_path()`），接入 `main.py` 与所有源文件。

### 4. 修复 B 站源时长解析 Bug

原代码对 B 站搜索结果 `duration` 直接调用 `int()`，但 B 站返回的是 `"5:27"` 字符串格式，导致 B 站源搜索永远报错、无法返回结果。已修复为时间字符串解析，B 站源恢复/可正常返回结果。

### 5. 文档补充

- `README.md` 新增 `proxy` 配置说明与专属日志章节
- 新增本文档说明本次改动内容

---

## 使用方法

### 配置代理

在配置文件或配置文件面板中设置：

```
proxy = http://127.0.0.1:7890
```

设置后所有音乐源请求均会走代理。

### 查看日志

```
plugin/logs/music.log
```

若网易云 / QQ 音乐搜索无结果，查看日志中对应源提示，可判断是否为风控（返回 `无结果` 或 HTTP 4xx）。

---

## 1.4.0 指令触发改造（2026-08-06）

### 背景：与多个点歌插件指令冲突

本机上同时部署了多个点歌插件（`astrbot_plugin_music`、`astrbot_plugin_listen_music` 与本插件），很容易互相抢指令（例如同时回复“点歌 xxx”），导致消息乱。

### 变化内容

| 项目 | 旧行为 | 新行为 |
| --- | --- | --- |
| 触发方式 | 正则关键词（`点歌`、`点歌姬`、`来首歌` 等免唤醒触发） | **标准指令** `@filter.command` |
| 强制前缀 | 无 | **必须 `/` 开头**（利用 AstrBot 唤醒前缀 `/`） |
| 唤醒要求 | 无（关键词直接触发） | 需以 `/` 唤醒（`is_at_or_wake_command`） |
| 优先级 | `priority=220` | **`priority=1000`**（全插件最高，先执行并 `stop_event()` 阻止其他插件抢） |
| 自定义别名 | 运行时正则合并 `aliases` 配置 | 启动时同步到 `CommandFilter.alias`（`_sync_order_aliases()`），`/song set aliases` 热更新 |

### 为什么“最高优先级 + stop_event”能解决打架

AstrBot 按 `priority` 从高到低执行 handler，`event.stop_event()` 后立即停止后续 handler：

- 本插件 `priority=1000` 最高，先被激活执行；
- `order_music` 执行结束会 `stop_event()`（原逻辑已有），阻塞 `astrbot_plugin_music` 等低优先级插件的同名指令；
- `/song` 管理指令保持 `priority=210`，`取消/序号选歌` 等交互正则保留，互不冲突。

### 实现要点

- 主入口：`main.py` 的 `order_music` 改为 `@filter.command("点歌", priority=1000)`
- 参数通过 `GreedyStr` 注入，函数签名 `(self, event, song_name)`，不再用正则抓取
- `_sync_order_aliases()`：把 WebUI/set 配置的 `aliases` 合并进已注册的 `CommandFilter`
- 帮助文本、README、metadata `help` 均已同步为 `/点歌` 用法
- 版本：`1.3.0` → `1.4.0`

### 注意

- 指令必须 `/` 前缀（默认中文别名已移除），自定义别名也需要 `/` 开头；
- 群聊中建议同时配置 @机器人或唤醒词，否则指令不触发；
- 若同时启用其他点歌插件，可将其卸载或将本插件 priority 保持最高。

### 1.4.1 修复：与唤醒前缀 `/` 冲突导致插件失效

部署环境的 AstrBot 全局 `wake_prefix` 为 `/`。AstrBot 的唤醒阶段会把以 `/` 开头的消息**剥离前缀**后再交给标准指令过滤器（`waking_check/stage.py`），因此注册为 `/点歌`、`/song` 的命令永远匹配不上剥离后的消息（`点歌 xxx`、`song xxx`），插件完全不可用。

**修复**：指令名注册为剥离后的形式（`点歌`、`song`）。由于唤醒本身要求 `/` 前缀，群聊中不带 `/` 的消息无法唤醒、`CommandFilter` 不会触发，**“必须用 `/` 点歌”的语义由唤醒机制天然保证**。

- 用户实际发送：`/点歌 歌名`、`/song fav` 等（不变）
- 注册/匹配：`点歌`、`song`（剥离 `/` 后）
- 自定义别名：配置写别名本体（如 `点歌吧`），用户发送 `/点歌吧` 即可
- 版本：`1.4.0` → `1.4.1`

### 1.4.2 修复：网易云播放失败（VIP/直链风控）

网易云 `outer/url` 直链已失效（2026 年起返回 HTML 反风控页），且无登录 Cookie 时 `enhance/player/url` 无法获取 VIP 歌曲直链（code=-110），导致网易云歌曲语音全部失败、回退坏卡片。

**修复**：

- `get_media_url` 改用明文接口 `api/song/enhance/player/url?ids=[id]&br=xxx`：
  - 免费歌曲返回真实直链（`m801/m701.music.126.net`），可正常语音播放
  - 音质映射：`super→320000`、`high→192000`、`low/standard→128000`
  - VIP/无版权歌曲（code=-110）返回空串，由主流程回退
- 回退优化（`_build_card`）：
  - QQ 音乐卡片仅对 `qqmusic` 源生成（此前网易云歌曲会发出 `songid=0` 的无效卡片）
  - 网易云歌曲拿不到直链时补发 `music.163.com/song?id=xxx` 网页链接兜底，点开可试听
- 新增 `netease_cookie` 配置项：填写网易云登录 Cookie（含 `MUSIC_U`）后，VIP 歌曲同样可获取直链（请求自动携带登录态，WebUI 修改后即时生效）
- 版本：`1.4.1` → `1.4.2`

### 已知限制

- 网易云 **VIP/无版权**歌曲在未配置 `netease_cookie` 时无法获取直链（服务端限制，需登录 Cookie 才能解），将回退为链接形式；
- 酷狗源播放地址接口当前不可用（`status=0`），点歌时会自动跳过该源，不影响其他源。

---

## 备注

- 本项目为自研点歌插件，致力于为 AstrBot 用户提供开箱即用、稳定可控的点歌功能
- 对云主机部署的风控问题，`proxy` 配置 + 专属日志是推荐方案